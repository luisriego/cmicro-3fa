version: '3.5'

services:
  cmicro-3fa:
    container_name: cmicro-3fa
    build:
      context: ./docker
      args:
        UID: $U_ID
    volumes:
      - ./:/var/www/html
    ###> XDEBUG 3 ###
    # Use your client IP here
    # Linux: run "ip a | grep docker0"
    # Windows (with WSL2) and Mac: host.docker.internal
    environment:
      XDEBUG_CLIENT_HOST: 172.17.0.1
      XDEBUG_CLIENT_PORT: 9003
      PHP_IDE_CONFIG: serverName=cmicro-3fa-server
    ports:
      - '1000:80'
    networks:
      - cmicro-3fa-network
    depends_on:
      - cmicro-3fa-mysql

  cmicro-3fa-mysql:
    container_name: cmicro-3fa-mysql
    image: mysql:8.2.0
    ports:
      - '3336:3306'
    environment:
      MYSQL_DATABASE: cmicro-3fa
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - cmicro-3fa-mysql-data:/var/lib/mysql
    networks:
      - cmicro-3fa-network
    command: [ 'mysqld', '--character-set-server=utf8mb4', '--collation-server=utf8mb4_unicode_ci' ]

networks:
  cmicro-3fa-network:

volumes:
  cmicro-3fa-mysql-data:
